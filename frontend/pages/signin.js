import AccountAccess from "../components/AccountAccess";
//add use user to this page, redirect if signed in, remove get started if signed in?
export default function SignInPage() {
    return (
       <AccountAccess />
    )
}